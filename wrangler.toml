name = "pem-buddys"
# Minimal config to satisfy Wrangler static site detection for Cloudflare Builds
# This points Wrangler to the public/ folder containing static assets.
site = { bucket = "public" }
# top-level main entry for Wrangler (required when using a site with modern Wrangler)
main = "workers-site/index.js"

# If Cloudflare project requires an account_id or other settings, add them in the dashboard or here.
account_id = "09af4e2d332aa82ae7ad5f6fbdb1e1c0"
# Set workers_dev = true to deploy to a workers.dev subdomain when appropriate
# workers_dev = true
