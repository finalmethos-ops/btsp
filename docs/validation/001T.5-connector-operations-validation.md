# 001T.5 Connector Operations Validation

Validated on 2026-06-28.

## Automated checks

- Backend suite: 143 tests passed.
- Connector lifecycle coverage includes due scheduling, duplicate tick prevention, exclusive claims, lease-token enforcement, exponential retries, dead-letter transition, expired leases, and operator replay.
- Frontend formatting, ESLint, TypeScript, and 9 Vitest tests passed.
- Ruff and repository whitespace validation passed.

## PostgreSQL migration

Migration `0020_connector_operations` was validated on a fresh PostgreSQL database by upgrading from empty to head, downgrading to `0019_connector_imports`, and upgrading to head again.

## Operational boundary

The API exposes durable scheduler and worker contracts. Production still requires separately supervised scheduler and transport-worker processes, an approved secret-provider adapter, metrics/alerts, and partner-specific connector configuration. No credentials are stored in execution records or returned by claim responses.
