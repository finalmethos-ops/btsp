# 001W.6 BTSP v1.0 Production Validation

Validation initiated on 2026-06-29.

## Existing evidence

- Packages 001W.1 through 001W.5 are documented with automated, migration, and live runtime evidence.
- The latest recorded 001W.5 gate reports 185 backend tests, 11 frontend tests, Ruff, formatting, ESLint, TypeScript, repository whitespace, migration round-trip, and nginx smoke checks passing.
- The live database was recorded at migration head `0028_audit_reporting` after core identity reconciliation.

## 001W.6 additions

- Added a read-only administration integrity audit and unit coverage for clean and drifted states.
- Consolidated the administrative security boundary and BTSP v1.0 release notes.

## Executed checks

- Backend suite: 187 tests passed under Python 3.12 on the host validation environment and again
  inside the freshly built backend image.
- Ruff lint passed; Ruff formatting identified and corrected seven files, after which all 195
  checked backend files passed.
- Installed-package consistency passed and the local dependency audit reported no known
  vulnerabilities.
- A fresh disposable PostgreSQL database completed `base -> 0028_audit_reporting -> base ->
  0028_audit_reporting` successfully.
- Development and production Compose configuration validation passed.
- The backend image built successfully from the current worktree.
- The live administration integrity audit passed with nine expected system roles, complete
  `SYSTEM_ADMIN` administration permissions, no duplicate active workflow definitions, and no
  unattributed administrative snapshots.
- Frontend formatting, ESLint, TypeScript, and 11 Vitest tests passed.
- A clean frontend image install used `npm ci`, reported zero vulnerabilities, completed the
  production build, and prerendered all 18 application routes.
- Public liveness/readiness probes and administration pages returned HTTP 200 through nginx;
  unauthenticated role, workflow, notification, diagnostics, audit, configuration, and user APIs
  returned HTTP 401.
- A custom-format PostgreSQL backup of the live database was listed and restored into an isolated
  database at `0028_audit_reporting`. Administration, receiving/reconciliation, and connector
  security integrity audits all passed against the restored copy.
- Validation scripts are now executable as backend modules, avoiding import-path failures from
  direct script-file execution in the production image.
- An isolated PostgreSQL/API environment verified successful administrator login and identity
  projection, plus authenticated access to role, workflow, notification, diagnostics, audit,
  configuration, user, and audit-export surfaces. The same requests from an authenticated
  zero-permission custom role returned HTTP 403. Invalid credentials returned HTTP 401, while
  system-role modification and assigned-role deletion returned HTTP 409.
- The release-seed idempotency regression now covers BPP and Independent workflows, approval and
  notification defaults, global configuration, core purchasing rules for both workflows, and
  purchase-order defaults. Entity counts remained stable across the second complete seed pass.
- Each durable application volume was archived read-only and restored into an isolated temporary
  volume. SHA-256 manifests matched for 3 attachment files and 9 purchase-order export files; the
  empty analytics-report volume also restored successfully. Temporary archives and restore
  volumes were removed after validation.

## Final candidate gate

The following must be rerun against the final clean commit before approval:

- [x] Backend dependency audit, package check, Ruff, formatting, full tests, and 001W integrity audit.
- [x] Frontend clean install, dependency audit, production build, formatting, ESLint, TypeScript,
  and Vitest.
- [x] Fresh PostgreSQL upgrade from base, full downgrade/upgrade rehearsal, and complete release
  seed idempotency regression.
- [x] Development and production Compose configuration validation.
- [x] Public routes, unauthenticated denial, authenticated administration APIs, least-privilege
  denials, invalid login, and protected role invariants.
- [x] Database backup/restore and durable attachment, purchase-order export, and analytics-report
  volume restoration.
- [x] Clean-worktree commit SHA, local content-addressed image IDs, and environment evidence.
- [ ] Required human approvals and immutable registry digests recorded.

## Release candidate artifacts

- Validated source commit: `586c7520b04e9b0aa61e3b2a58bd8e4eeaf86102`
- Backend image ID: `sha256:bfd46a50a59f4f7c1310666e115faec662a9a4086452eb8e4a157e0787ea4dc9`
- Frontend image ID: `sha256:1050e777535da198e2a742f949e2c20fa8864d094b366fab901dfe58d4d30dff`
- Direct image validation: 187 backend tests with Ruff and 11 frontend tests passed.
- Backend RC: `ghcr.io/finalmethos-ops/btsp-backend:v1.0.0-rc.1@sha256:bfd46a50a59f4f7c1310666e115faec662a9a4086452eb8e4a157e0787ea4dc9`
- Frontend RC: `ghcr.io/finalmethos-ops/btsp-frontend:v1.0.0-rc.1@sha256:1050e777535da198e2a742f949e2c20fa8864d094b366fab901dfe58d4d30dff`

The source commit was clean when the images were built and tested. The content-addressed RC images
were published to GHCR and their remote OCI indexes verified. Required human approvals remain
outstanding, so this record does not declare production approval.

## 2026-07-02 full-system hardening revalidation

- Audited the actual current tree through migration `0047_store_read`, 194 API operations, and 34
  frontend routes.
- Upgraded `pypdf` from vulnerable 5.9.0 to 6.14.2. Both installed-environment and declared-file
  `pip-audit` checks report no known vulnerabilities; npm reports zero vulnerabilities.
- Removed stale local `python-jose`, split development dependencies from the runtime image, and
  removed unused compiler and test tooling from production.
- Removed unused workflow compatibility maps and the redundant public snapshot API. Added an
  OpenAPI regression test that fails if any route becomes public outside the explicit probe,
  version, login, and one-time bootstrap allow-list.
- Hardened JWT claims, login throttling, disable-after-use bootstrap, password/input bounds, CORS, browser
  headers, upload byte/page/row limits, and durable invoice-intake storage.
- Ruff lint/format, Bandit, Python compilation, package consistency, and 219 backend tests passed.
- Frontend formatting, ESLint, strict TypeScript with unused-code checks, 11 Vitest tests, npm
  audit, and the 34-route production build passed.
- Fresh PostgreSQL completed `base -> 0047_store_read -> base -> 0047_store_read`; development and
  production Compose configurations passed.
- Rebuilt backend/frontend images run as UID 10001 and UID 1000. The live stack reports healthy
  dependencies and all four durable storage roots, omits disabled bootstrap with HTTP 404, rejects
  the ninth failed login with HTTP 429, and returns HTTP 404 for the removed snapshot route.
- Administration, receiving/reconciliation, and vendor-connector integrity audits passed against
  the live database.

Human approval and immutable remote image digests remain release-process requirements; this
technical revalidation does not substitute for them.

## 2026-07-26 current-tree revalidation

- The live database and source migration graph both report
  `0113_sub_event_membership_roles` as the single Alembic head.
- The administration integrity audit passed with 16 expected system roles,
  complete `SYSTEM_ADMIN` permissions, no duplicate active workflow versions,
  and no unattributed administrative snapshots.
- The complete backend suite passed with 299 tests.
- The frontend suite passed with 49 tests across 16 files; ESLint and strict
  TypeScript checks also passed.
- Updated PostCSS to 8.5.23 and constrained vulnerable transitive
  `minimatch`/`brace-expansion` versions to patched releases. The complete npm
  dependency graph now reports zero known vulnerabilities.
- Production dependency audit (`npm audit --omit=dev`) and the full
  development dependency audit both report zero known vulnerabilities.

This remains technical evidence only. A clean immutable candidate, current
backup/restore rehearsal, environment review, registry digests, and human
approvals are still required before production approval.

## 2026-07-28 production-candidate stabilization

- Source and the live development database both report
  `0116_notification_action_href` as the single Alembic head.
- Ruff lint and formatting, Python compilation, installed-package consistency,
  Bandit, and the complete backend suite passed. The suite now contains 301
  passing tests.
- The administration, receiving/reconciliation, and vendor-connector integrity
  audits passed against the current development database.
- The declared backend runtime dependency set and both the frontend production
  and development dependency graphs report zero known vulnerabilities.
- A clean frontend dependency installation passed Prettier, ESLint, strict
  TypeScript, 49 Vitest tests across 16 files, and the 49-route Next.js
  production build.
- Production backend and frontend images built from the current source.
  Development-only Python dependencies are explicitly disabled in the
  production backend build.
- An isolated production Compose project booted with non-root containers,
  read-only backend/frontend root filesystems, all Linux capabilities dropped,
  and HTTP 200 responses from liveness, readiness, and event-login routes.
- A fresh isolated PostgreSQL database completed
  `base -> 0116_notification_action_href -> base ->
  0116_notification_action_href`.
- A custom-format stream of the current development database restored
  successfully into an isolated PostgreSQL database with 114 public tables and
  migration head `0116_notification_action_href`.
- CI now enforces the Bandit application scan in addition to dependency audit,
  Ruff, formatting, migrations, tests, and the frontend gate.

The technical candidate is green and has been consolidated into a clean local
release revision after the checks above. Secret provisioning/rotation,
production infrastructure and TLS configuration, immutable registry digests,
monitoring/alert destinations, and required human approvals remain deployment
gates.

## 2026-08-03 security-evidence and monitoring validation

- Nginx and FastAPI request IDs were correlated through the public Cloudflare
  path without logging query strings or request payloads.
- All six production containers use bounded compressed log retention, and the
  recurring host watchdog recovered to healthy after a verified encrypted R2
  backup and disposable restore exercise.
- Canonical login, administrative-action, and permission-change audit producers
  passed focused identity tests and the complete backend regression suite.
- Ruff lint and formatting passed across the backend; the suite contains 319
  passing tests.
- The non-disclosing production secret audit passed all required controls,
  including default/strength/reuse checks, database credential consistency,
  Git exclusion, HTTPS/CORS and loopback-origin restrictions, protected-file
  presence, and authoritative Windows ACL review. Historical rotation dates
  remain an explicitly recorded governance warning pending the next approved
  rotation window.
- All six Compose service images are pinned by SHA-256 digest, and the running
  PostgreSQL, Redis, backend, frontend, Nginx, and Cloudflare Tunnel containers
  match their configured immutable image digests exactly.
- The backup workflow no longer stops public services. Its two durable-file
  passes bracket PostgreSQL's consistent dump so a terminated Windows task
  cannot leave Nginx or the backend offline.
- The corrected scheduled workflow produced and encrypted a fresh bundle,
  passed a disposable 114-table restore at Alembic head
  `0116_notification_action_href`, uploaded and content-verified the bundle in
  R2, and completed with 46/46 concurrent public probes returning HTTP 200.
- Docker build checks pass for both digest-pinned application Dockerfiles, and
  CI now enforces those checks before Compose validation. The guarded release
  builder rejects missing semantic versions and dirty source trees before any
  image build begins.

## 2026-08-04 clean release-candidate gate

- The complete backend suite passed with 320 tests. Python compilation,
  package consistency, Ruff lint/formatting, Bandit, and the declared runtime
  dependency audit also passed with no known vulnerabilities.
- A disposable PostgreSQL database completed the full
  `base -> 0116_notification_action_href -> base ->
  0116_notification_action_href` migration rehearsal and was removed.
- A clean disposable frontend installation passed Prettier, ESLint, strict
  TypeScript, all 49 Vitest tests, and the 49-route Next.js production build.
- `brace-expansion` was advanced from vulnerable 5.0.8 to patched 5.0.9; both
  the complete and production dependency graphs report zero known
  vulnerabilities.
- Development and public-production Compose rendering, both Nginx
  configurations, Dockerfile checks, Bash syntax, PowerShell parsing,
  repository whitespace, application integrity audits, and the live
  production-readiness gate passed.

## 2026-08-04 `v1.0.0-rc.23` production promotion

- A fresh encrypted pre-deployment backup passed its checksum, disposable
  114-table restore at Alembic head `0116_notification_action_href`, and
  private R2 upload.
- The backend and frontend images built from revision
  `e24f7ac9259c3cccd3155c856b95c30c46e79e03` were published and promoted by
  immutable SHA-256 digest.
- Backend replacement, migration verification, and frontend replacement each
  reached healthy state before the next deployment phase proceeded.
- Public homepage, event-login, liveness, and readiness probes returned HTTP
  200 after deployment. No new application error, exception, or traceback was
  present in the targeted container-log review.
- The complete production-readiness gate passed, including secret controls,
  exact six-service inventory, configured-versus-running digest integrity,
  Cloudflare routing, security headers, container health, and scheduled-backup
  status.

## 2026-08-04 login-free projector promotion

- The projector display now uses a 24-hour, read-only token scoped to one
  sub-event instead of an interactive user login. The token is transferred in
  the URL fragment, moved into tab-scoped storage, and removed from the address
  bar before any display-data request.
- Public projector responses exclude presenter notes and slide queues. Images,
  branding, and presentation state require the same scoped token; control,
  analytics, ordering, and administration endpoints retain user authorization.
- The complete backend suite passed with 323 tests. The frontend passed ESLint,
  strict TypeScript, all 50 tests, and a 49-route production build. Dependency
  audits and the backend security scan reported no vulnerabilities or findings.
- `v1.0.0-rc.24` was built from revision
  `07263766c4eed641e7aaf7e33034fdc312dbdf42`, provenance-verified, and exercised
  in an isolated production-mode stack before promotion.
- A fresh encrypted backup passed checksum, disposable 114-table restore at
  Alembic head `0116_notification_action_href`, private R2 upload, and remote
  content comparison immediately before deployment.
- Through the public Cloudflare route, the projector page returned HTTP 200,
  the display API returned 401 without a projector token, and a valid scoped
  token returned a sanitized HTTP 200 response. Twenty concurrent readiness
  probes passed, targeted logs contained no new application errors, and the
  complete production-readiness gate passed with zero secret-audit warnings.

## 2026-08-04 production bootstrap shutdown

- Production now sets `BOOTSTRAP_ENABLED=false`. The installation-only
  administrator route is not registered, is absent from the production OpenAPI
  schema, and returns HTTP 404 through the public Cloudflare route.
- The production secret audit fails if bootstrap is not explicitly disabled;
  its current run passed with zero failures and zero warnings.
- The complete backend suite passed with 324 tests. Ruff lint/formatting,
  Bandit, Python compilation, and the declared runtime dependency audit passed
  with no known vulnerabilities.
- `v1.0.0-rc.25` was built from revision
  `5feec0aa05237b4a1dfea2673699d2839c92cb98`, provenance-verified, and exercised
  against disposable PostgreSQL and Redis services before promotion.
- The pre-deployment encrypted backup passed checksum validation, a disposable
  114-table restore at Alembic head `0116_notification_action_href`, R2 upload,
  and remote content verification.
- After promotion, 20/20 concurrent readiness probes returned HTTP 200, public
  application and event pages returned HTTP 200, the bootstrap URL returned
  HTTP 404, targeted application logs contained no errors, and all six running
  containers matched their configured immutable image digests.

## 2026-08-04 restrictive browser content policy

- The public Nginx layer now applies an explicit same-origin default policy and
  limits scripts, styles, images, fonts, connections, frames, workers, media,
  manifests, and form submissions to the sources required by BTSP.
- Blob/data allowances are limited to application features that render fetched
  product images, venue PDFs, QR codes, and service-worker assets. External
  script and API destinations remain blocked; framing and plug-in content remain
  prohibited.
- The public health gate validates every required CSP directive independently,
  in addition to the existing Cloudflare, liveness, readiness, and security
  header checks.
- Nginx configuration validation passed before reload. The complete production
  readiness gate passed afterward, and headless Edge rendered the event login
  with hydrated content and no CSP violations in the browser log.

## 2026-08-04 bounded access-token lifetime

- New interactive access tokens expire after 15 minutes; production startup
  rejects configurations above 30 minutes, and the production secret audit
  independently enforces the 5-to-30-minute boundary. Refresh sessions remain
  rotating and retain their existing 14-day lifetime.
- The complete backend suite passed with 325 tests. Ruff lint and formatting,
  Bandit, and the declared dependency audit passed with no findings or known
  vulnerabilities.
- `v1.0.0-rc.26` was built from revision
  `97e24a0ad637eeaf053268be474cce9fa2a2b659`. The backend is pinned to
  `sha256:a00950323db325230bf26cbd6ea7520678838009495a2be9b1886619db085efc`
  and the frontend to
  `sha256:3535bf207c22aa201dd40719b21ca9a82534a9c6a272a37492a6758b7a35e0b3`.
- The pre-deployment encrypted backup passed checksum validation, a disposable
  114-table restore at Alembic head `0116_notification_action_href`, private R2
  upload, and remote content comparison.
- After promotion, the running backend reported a 15-minute setting and issued
  a token with an exact 900-second lifetime. Public health and readiness passed,
  all six services were healthy and matched their configured immutable digests,
  Cloudflare remained reachable, the scheduled backup task was healthy, and the
  complete production-readiness gate passed with zero secret-audit warnings.

## 2026-08-05 bounded refresh-session lifetime

- Refresh-token rotation now preserves the original session expiration instead
  of opening a new 14-day window. Production rejects refresh lifetimes above 14
  days, and the production secret audit independently enforces the boundary.
- The complete backend suite passed with 325 tests. Ruff lint and formatting,
  package consistency, Bandit, and the declared dependency audit passed with no
  findings or known vulnerabilities.
- `v1.0.0-rc.27` was built from revision
  `4e3095197d5df3909f9dd71f7a13e05e00dcb6f3`, provenance-verified, and
  published with backend digest
  `sha256:ed1f44fe8a64060a212c19dbc59092278202b21f5f6884012d894e28c6c2ca3e`
  and frontend digest
  `sha256:8409528e2d01eaba34552517bd6f5dc51064bd1f3c1b9fd2aebce5a637824e90`.
- The pre-deployment encrypted backup passed checksum validation, a disposable
  114-table restore at Alembic head `0116_notification_action_href`, private R2
  upload, and remote content comparison.
- After promotion, the running backend reported the intended 15-minute access
  and 14-day refresh limits. Public application, event-login, liveness, and
  readiness probes returned HTTP 200; 20/20 concurrent readiness probes passed;
  bootstrap remained unavailable with HTTP 404; all six services were healthy
  and matched their immutable digests; and the complete production-readiness
  gate passed with zero secret-audit warnings.

## 2026-08-05 browser request responsiveness

- The duplicate development Compose project was stopped, removing its
  reload-mode source polling, failed gateway restart loop, and approximately
  350 MiB of avoidable resident memory while preserving its stopped containers
  and volumes. The named six-service production project remained online.
- Concurrent identical authenticated GET requests now share one in-flight
  parsed response and discard it immediately after completion; no persistent
  business-data cache was introduced. Concurrent expired requests also share a
  single refresh-token rotation, preventing redundant calls and refresh races.
- Prettier, ESLint, strict TypeScript, all 52 frontend tests, both npm dependency
  audits, and the 49-route production build passed with zero vulnerabilities.
- `v1.0.0-rc.28` was built from revision
  `2c2dbb90341d40f614c135a3b9f2fe08f58df7f5` and promoted with backend digest
  `sha256:d03da9d62fd2f4c4382a31496b0c458cc87a26889f4a79e92068d30a4aeb78c2`
  and frontend digest
  `sha256:3c10c179975ae1e585eedba89950fc2da2163b9b27cac29ccfd78e181b422b11`.
- The pre-deployment encrypted backup passed checksum validation, disposable
  114-table restore at Alembic head `0116_notification_action_href`, private R2
  upload, and remote comparison. After promotion, public probes completed in
  approximately 0.12–0.13 seconds, 40/40 concurrent readiness requests passed,
  recent application logs contained no fatal errors, and the complete
  production-readiness gate passed.

## 2026-08-05 vendor-scoped model numbers

- Catalog model identity is now the vendor and model-number pair. Different
  vendors may publish the same model number, while duplicate model numbers
  within one vendor remain prohibited.
- Administrator catalog imports, vendor workbook imports, vendor model edits,
  and event-order release all resolve products within vendor scope. Existing
  purchasing, cost-history, receiving, and reporting references retain their
  stable internal product keys when a vendor edits a displayed model number.
- The complete backend suite passed with 329 tests. Ruff lint and formatting,
  Python compilation, package consistency, Bandit, the full reversible
  migration chain, and an `0116 -> 0117` migration rehearsal against a
  disposable restore of current production data all passed. Alembic reported
  no schema drift afterward.
- `v1.0.0-rc.29` was built from revision
  `3f7b10c97fbdb3bf3dcadc35fe4dff8b1f607190` and promoted with backend digest
  `sha256:fa3a07a57f643bc394f3ade23c2597d0cf909a011c9aa6b4904cbcb688567f83`
  and frontend digest
  `sha256:87e12477cd0acf38867d9ff2b4b1adf5b1361128bd7e7acf1200502c181aec5b`.
- The pre-deployment encrypted backup passed checksum, disposable restore,
  private R2 upload, and remote comparison at Alembic head `0116`. A second
  post-deployment encrypted backup passed the same checks at
  `0117_vendor_scoped_models`. Public liveness, readiness, application,
  security-header, secret-control, tunnel, backup-task, and immutable-image
  checks all passed after promotion.

## 2026-08-05 user-facing model-number continuity

- Purchase requests, purchase orders, vendor PO workspaces, receiving,
  backorders, invoice reconciliation, PO PDF/CSV/JSON artifacts, and executive
  best-seller cards now display the vendor's model number. Stable collision-safe
  product keys remain in API payloads and machine-readable exports for internal
  matching and audit without replacing the user-facing identifier.
- Executive best-seller results also retain vendor context, so the same model
  number from multiple vendors remains distinct. A regression test verifies an
  order whose internal product key deliberately differs from its model number.
- The complete backend suite passed with 329 tests. Ruff lint and formatting,
  ESLint, strict TypeScript, all 52 frontend tests, and the 49-route production
  build passed. This release required no database migration; Alembic remains at
  `0117_vendor_scoped_models`.
- `v1.0.0-rc.30` was built from revision
  `39b7e763eb039b7cfa463ee5b099b331dac31752` and promoted with backend digest
  `sha256:959af1ef917b28f428e53c6fb58668cbc744b28f6874e4e1abe5a75204caf791`
  and frontend digest
  `sha256:9719e26b1af414c39b317cb315a6223478693a29030067d3839235c1c05563a6`.
- Both pre- and post-deployment encrypted backups passed checksum validation,
  disposable 114-table restores, private R2 upload, and remote comparison.
  Public health, readiness, application, security-header, secret-control,
  tunnel, scheduled-backup, and immutable-image checks passed; 20/20 concurrent
  readiness probes returned HTTP 200 and recent service logs contained no fatal
  application errors.

## 2026-08-05 case-insensitive vendor MOQ imports

- Vendor model imports now match MOQ codes without regard to capitalization.
  This resolves the false `Unknown MOQ code STANDARD` rejection when an existing
  vendor rule is stored as `Standard`; genuinely unknown codes remain rejected.
- A mixed-case, multiple-rule regression test was added. The complete backend
  suite passed with 330 tests, and Ruff lint and formatting passed.
- `v1.0.0-rc.31` was built from revision
  `81b9f78812c172da6076b268e33c4317cf089980` and promoted with backend digest
  `sha256:5bf6dfe6fa3ea1de775ebe1705d9e4d069ee7f5a49600f1b2d0c9ded92d233e2`
  and frontend digest
  `sha256:2f292db7cd28f1031523ef11291e417fc90385f208651a55140fc9da53e74932`.
- Pre- and post-deployment encrypted backups passed checksum validation,
  disposable 114-table restores at `0117_vendor_scoped_models`, private R2
  upload, and remote comparison. Public application and readiness checks,
  immutable-image verification, and the production security audit all passed.

## 2026-08-05 approved MOQ workbook dropdowns

- Vendor model Excel exports now apply a dropdown to the `moq_code` column for
  all 50,000 supported model rows. The choices come only from the selected
  vendor's active MOQ rules; a hidden named-range sheet keeps long rule lists
  compatible with Excel while preventing unsupported free-form entries.
- The existing web model editor was verified to use the same active-rule list.
  Historical inactive assignments remain exportable but are not offered for new
  workbook selections.
- The complete backend suite passed with 330 tests, including workbook
  validation-list coverage. Ruff lint and formatting and the 49-route frontend
  production build passed.
- `v1.0.0-rc.32` was built from revision
  `92754287e4189e304439b3a92413daa1e7b1ce96` and promoted with backend digest
  `sha256:0ede13acc20dfb249074eea36fe2be8f89563aa3d20095295e2830df411e27a5`
  and frontend digest
  `sha256:d07108dd154e1c26f33a35dc08290f73a687b86791b716e5f7d893e7e78abaa6`.
- Pre- and post-deployment encrypted backups passed checksum validation,
  disposable 114-table restores, private R2 upload, and remote comparison.
  Public health, readiness, security, tunnel, scheduled-backup, and immutable
  image checks passed after promotion.
