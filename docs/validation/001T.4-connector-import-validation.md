# 001T.4 Connector Import Validation

Validated on 2026-06-28.

## Automated checks

- Ruff check and formatting: passed.
- Backend suite: 141 tests passed.
- Connector cases cover JSON batches, event linkage, checksum replay, empty and malformed input, media-type rejection, empty batches, EDI fail-closed behavior, and partial event conflicts.

## PostgreSQL migration

Migration `0019_connector_imports` was validated against a fresh PostgreSQL database:

1. upgraded from an empty database to head;
2. confirmed `0019_connector_imports (head)`;
3. downgraded to `0018_shipments_asns`;
4. upgraded to head again.

The temporary audit database was removed after validation.

## Security and integrity boundaries

- Upload reads stop at 10 MiB plus one detection byte.
- Endpoint identity is server-controlled.
- Only active inbound/bidirectional endpoints accept imports.
- File replay is scoped by endpoint and raw-content SHA-256.
- Unsupported EDI input is rejected without heuristic parsing.
- Connector credentials remain references; imports do not persist secrets.
