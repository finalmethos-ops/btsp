# 001T.1 Vendor Integration Foundation Validation

## Validation metadata

- Validation date: 2026-06-28
- Database: PostgreSQL 16
- Backend runtime: Linux, Python 3.12
- Migration head: `0016_vendor_integrations`

## Results

| Area | Result | Evidence |
| --- | --- | --- |
| Backend lint and format | Pass | Ruff clean across app, tests, scripts, and Alembic |
| Backend tests | Pass | 125 tests passed with deprecations elevated to errors |
| Fresh migration | Pass | Empty PostgreSQL database upgraded through `0016` |
| Migration reversibility | Pass | `0016` downgraded to `0015` and re-upgraded successfully |
| Production migration | Pass | Deployed PostgreSQL database upgraded to `0016` |
| Authentication boundary | Pass | Unauthenticated event listing returned HTTP 401 |
| Restart persistence | Pass | Endpoint and inbound event remained present after backend restart |

## Live PostgreSQL scenario

The repeatable validator at `backend/scripts/validate_001t_foundation.py`:

1. Created a uniquely tagged active catalog vendor.
2. Registered an inbound REST API endpoint with an opaque connection reference.
3. Recorded an acknowledgement envelope and canonical SHA-256 payload digest.
4. Replayed the identical external event ID and received the original record.
5. Confirmed one event row rather than a duplicate.
6. Confirmed endpoint and event receipt snapshots.
7. Restarted the backend and verified endpoint/event persistence.

## Decision

**GO for package 001T.2 — Purchase Order Acknowledgements.**

This decision validates the internal authenticated ingestion foundation only. It does not claim a
public vendor webhook, connector authentication, external network delivery, acknowledgement domain
projection, shipment handling, ASN processing, EDI translation, or scheduled import automation.
